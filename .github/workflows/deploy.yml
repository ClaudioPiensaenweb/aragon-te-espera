name: Deploy Aragón Te Espera
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Redeploy en Dokploy
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Dokploy webhook
        run: |
          curl -s -X POST "http://185.14.58.119:3000/api/deploy/compose/vflH80Sa-81qS4zZe2Lhd" \
            --fail-with-body
          echo "Deploy lanzado OK"
